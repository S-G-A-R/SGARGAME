@page "/MenuCategorias"

<div class="category-map-container">
    <div class="map-path">

        <div class="hanging-sign">
            <img src="/imagenes/cartel1.png" class="sign-img" alt="SGAR GAME">
        </div>

        <div class="category-node" style="left: 45%; top: 25%;">
            <a href="/categoria1">
                <div class="node-icon category-1-icon">
                    <img src="imagenes/question.png" alt="Aprender sobre reciclaje" />
                </div>
                <span class="node-label">Categoría 1: Aprende sobre reciclaje</span>
            </a>
        </div>

        <div class="category-node" style="left: 30%; top: 48%;">
            <a href="/nivel1">
                <div class="node-icon category-2-icon">
                    <img src="imagenes/papelerasgame.png" alt="Ordena y Recicla" />
                </div>
                <span class="node-label">Categoría 2: Ordena y Recicla</span>
            </a>
        </div>

        <div class="category-node" style="left: 45%; top: 71%;">
            <a href="/GameReciclaje">
                <div class="node-icon category-3-icon">
                    <img src="imagenes/portapapeles.png" alt="Evaluación del Guardián" />
                </div>
                <span class="node-label">Categoria 3: Evaluación del Guardián</span>
            </a>
        </div>

        @* CONTENEDOR DEL MAPACHE AJUSTADO A LA NUEVA POSICIÓN DEL MAPA *@
        <div class="raccoon-dialog-container" style="left: 65%; top: 60%;">
            <p class="dialog-bubble">¡Este es el camino para ser un Guardián del Reciclaje!</p>
            <img src="/imagenes/mapachefelix.png" class="raccoon-img" />
        </div>

    </div>
</div>

<style>
    /* ------------------------------------- */
    /* FUENTE Y FONDO */
    /* ------------------------------------- */
    @@import url('https://fonts.googleapis.com/css2?family=Baloo+2:wght@500;700&family=Poppins:wght@400;600&display=swap');

    html, body {
        margin: 0;
        padding: 0;
        height: 100%;
        width: 100%;
        overflow: hidden; /* evita cualquier borde */
        font-family: 'Poppins', sans-serif;
    }

    /* Fondo sin bordes */
    .category-map-container {
        position: fixed;
        inset: 0;
        width: 100vw;
        height: 100vh;
        background: url("/imagenes/bosquee.png") no-repeat center center; /* ← usa ruta absoluta */
        background-size: cover;
        background-position: center;
        overflow: hidden;
        z-index: 0; /* Base */
    }

        /* ✔ Capa translúcida mejorada (DETRÁS del contenido) */
        .category-map-container::before {
            content: "";
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, 0.35); /* ← Cambia opacidad/color aquí */
            backdrop-filter: blur(2px); /* Efecto difuso suave */
            z-index: -1; /* ← AHORA SÍ va detrás del contenido */
        }

    /* Contenido encima */
    .map-path {
        position: relative;
        z-index: 5; /* Siempre arriba */
        width: 100%;
        height: 100%;
    }


    /* ------------------------------------- */
    /* CÍRCULOS DE CATEGORÍAS */
    /* ------------------------------------- */
    .category-node {
        position: absolute;
        text-align: center;
        cursor: pointer;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 200px;
        height: 200px;
        background-color: rgba(255, 255, 255, 0.9);
        border-radius: 50%;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.25);
        border: 3px solid #2c8c4a;
        z-index: 3;
        transition: transform 0.3s ease, box-shadow 0.3s;
        animation: floating 4s ease-in-out infinite;
    }

        .category-node:hover {
            transform: translateY(-8px) scale(1.05);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
        }

    @@keyframes floating {
        0%, 100% {
            transform: translateY(0);
        }

        50% {
            transform: translateY(-8px);
        }
    }

    .category-node a {
        text-decoration: none;
        color: #222;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100%;
    }

    .node-icon {
        width: 70px;
        height: 70px;
        margin-bottom: 8px;
        border-radius: 50%;
        background-color: #f0f0f0;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .node-icon img {
            max-width: 80%;
            max-height: 80%;
        }

    .node-label {
        font-weight: bold;
        font-size: 0.9em;
    }

    /* ------------------------------------- */
    /* MAPACHE Y BURBUJA - AJUSTADO */
    /* ------------------------------------- */
    .raccoon-dialog-container {
        position: absolute;
        display: flex;
        align-items: flex-end;
        justify-content: center;
        transform: translate(-50%, -50%);
        z-index: 5;
        /* el mapache un poco más cerca del centro */
        top: 63%;
        left: 63%;
    }

    /* 🔹 Tamaño del mapache más grande */
    .raccoon-img {
        width: 160px;
        height: auto;
        animation: wagTail 3s infinite ease-in-out;
    }

    /* 🔹 Burbuja más clara y ligera */
    .dialog-bubble {
        position: absolute;
        background: rgba(255, 255, 255, 0.6); /* más transparente */
        border: 2px solid rgba(44, 140, 74, 0.4); /* borde más suave */
        border-radius: 14px;
        padding: 12px 16px;
        max-width: 160px;
        text-align: center;
        font-size: 0.8em;
        font-weight: 600;
        color: #1a1a1a;
        backdrop-filter: blur(4px); /* efecto cristal suave */
        /* 🔹 Más cerca del mapache */
        bottom: 120px;
        right: -70px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

        .dialog-bubble::after {
            content: '';
            position: absolute;
            bottom: -10px;
            right: 50px;
            width: 0;
            height: 0;
            border: 8px solid transparent;
            border-top-color: rgba(44, 140, 74, 0.4);
        }

        .dialog-bubble::before {
            content: '';
            position: absolute;
            bottom: -7px;
            right: 50px;
            width: 0;
            height: 0;
            border: 6px solid transparent;
            border-top-color: rgba(255, 255, 255, 0.6);
            z-index: 1;
        }

    /* 🔹 Animación del mapache */
    @@keyframes wagTail {
        0%, 100% {
            transform: rotate(0deg);
        }

        50% {
            transform: rotate(3deg);
        }
    }

    /* ============================================================
                📱 RESPONSIVE – Ajuste para pantallas pequeñas (USO DE VW)
                ============================================================ */

    /* Tablets (pantallas medianas) */
    @@media (max-width: 1024px) {
        .category-node {
            width: 160px;
            height: 160px;
        }

        .node-icon {
            width: 60px;
            height: 60px;
        }

        .raccoon-img {
            width: 130px;
        }

        .dialog-bubble {
            font-size: 0.75em;
            max-width: 150px;
            right: -20vw;
            bottom: 100px;
        }
    }

    /* 🔥 AJUSTES CLAVE para Celulares (max-width: 768px) - CORRECCIÓN DEFINITIVA 🔥 */
    @@media (max-width: 768px) {
        .game-title {
            font-size: 8vw;
        }

        .subtitle {
            font-size: 4vw;
        }
        /* NODO Y TEXTO ADAPTABLES CON VW */
        .category-node {
            width: 32vw;
            height: 32vw;
            border: 3px solid #2c8c4a;
            transform: translate(-50%, -50%);
        }

        .node-icon {
            width: 12vw;
            height: 12vw;
            margin-bottom: 5px;
        }

        .node-label {
            font-size: 3.3vw;
            padding: 0 2vw;
        }
        /* POSICIONES DEL MAPA NO LINEAL EN MÓVIL */
        .category-node:nth-child(2) {
            left: 50% !important;
            top: 25% !important;
        }
        /* CATEGORÍA 2 ALEJADA DEL MAPACHE */
        .category-node:nth-child(3) {
            left: 20% !important; /* Movido a la izquierda para el carril central */
            top: 50% !important;
        }

        .category-node:nth-child(4) {
            left: 50% !important;
            top: 75% !important;
        }
        /* MAPACHE Y BURBUJA: POSICIONADO MÁS A LA IZQUIERDA (70%) */
        .raccoon-dialog-container {
            width: 38vw;
            height: 45vw;
            left: 70% !important; 
            top: 60% !important;
            transform: translate(-50%, -50%);
            display: block;
            position: absolute;
        }

        .raccoon-img {
            width: 20vw;
            height: auto;
            position: absolute;
            bottom: 0;
            right: 0; /* Mapache en la esquina inferior derecha del contenedor */
            left: auto;
            transform: none;
            animation: wagTail 3s infinite ease-in-out;
        }
        /* BURBUJA: A LA IZQUIERDA DEL MAPACHE (esquina superior izquierda del contenedor) */
        .dialog-bubble {
            position: absolute;
            top: 0;
            left: 0;
            font-size: 3vw;
            max-width: 25vw; 
            padding: 10px 8px;
            right: auto;
            bottom: auto;
            transform: none;
        }
    }
    /*Pueba de cambios. */

    /* Celulares pequeños (max-width: 480px) */
    @@media (max-width: 480px) {
        .category-node {
            width: 38vw;
            height: 38vw;
        }

            .category-node:nth-child(3) {
                left: 15% !important; /* Más a la izquierda en pantallas pequeñas */
            }

        .raccoon-img {
            width: 25vw;
        }

        .raccoon-dialog-container {
            left: 75% !important; 
            top: 60% !important;
        }

        .dialog-bubble {
            font-size: 3.2vw;
            max-width: 32vw;
            right: -20vw;
            right: -20vw;
        }
    }

    /* Contenedor del letrero */
    .hanging-sign {
        position: absolute;
        top: -30px; /* Lo sube hacia arriba para que parezca colgado */
        left: 50%;
        transform: translateX(-50%);
        width: 480px; /* Tamaño del letrero - AJUSTA */
        z-index: 10; /* No bloquea las categorías */
        pointer-events: none; /* Evita interferir al dar clic */
    }

    /* Imagen */
    .sign-img {
        width: 100%;
        height: auto;
        display: block;
    }

    /* Cuerdas */
    .hanging-sign::before,
    .hanging-sign::after {
        content: "";
        position: absolute;
        top: -60px; /* Cuerdas más arriba */
        width: 6px;
        height: 70px; /* Largo de la cuerda */
        background: #c29f7b; /* Color cuerda */
        border-radius: 3px;
    }

    /* Cuerda izquierda */
    .hanging-sign::before {
        left: 25%;
    }

    /* Cuerda derecha */
    .hanging-sign::after {
        right: 25%;
    }
</style>